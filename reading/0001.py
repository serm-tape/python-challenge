# What this function do?

def doSomething(numbers):
    if len(numbers) != 2:
        return None
    else
        a = numbers[0]
        numbers[0] = numbers[1]
        numbers[1] = a

    return numbers

# doSomething([1, 2])
    